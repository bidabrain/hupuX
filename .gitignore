# ── 编译输出 ──────────────────────────────────────────────
build/
app/build/
**/build/

# ── Gradle 缓存 ───────────────────────────────────────────
.gradle/

# ── 本地配置（含 SDK 路径，每台机器不同）────────────────────
local.properties

# ── IDE 文件（Android Studio / IntelliJ）────────────────────
.idea/
*.iml
*.ipr
*.iws

# ── macOS 系统文件 ────────────────────────────────────────
.DS_Store

# ── 签名 keystore（包含私钥，绝对不能上传）────────────────
*.jks
*.keystore

# ── 性能分析 / 截图抓取 ───────────────────────────────────
captures/
profiling/
